# 오늘 테스트할 수 있는 항목 가이드

이 문서는 프로젝트를 시작한 첫날 테스트할 수 있는 항목들을 단계별로 안내합니다.

## 🎯 테스트 목표

오늘의 목표는 다음을 확인하는 것입니다:
1. ✅ 프로젝트가 정상적으로 실행되는지
2. ✅ 웹 인터페이스가 제대로 표시되는지
3. ✅ 테미 로봇 연결 준비가 되었는지
4. ✅ (로봇이 있다면) 실제 연결 테스트

---

## 1단계: 프로젝트 실행 테스트

### 의존성 설치

터미널에서 다음 명령어를 실행하세요:

```bash
cd /Users/activejang/Documents/code/temi
npm install
```

**예상 시간**: 1-2분

**성공 확인**: `node_modules` 폴더가 생성되고 오류가 없어야 합니다.

### 개발 서버 실행

```bash
npm run dev
```

**예상 시간**: 10-20초

**성공 확인**: 
- 터미널에 다음과 같은 메시지가 표시됩니다:
  ```
  ▲ Next.js 14.x.x
  - Local:        http://localhost:3000
  - Ready in Xs
  ```

### 브라우저에서 확인

1. 웹 브라우저를 엽니다 (Chrome, Safari, Firefox 등)
2. 주소창에 `http://localhost:3000` 입력
3. Enter 키를 누릅니다

**예상 결과**:
- ✅ "테미 로봇 제어 시스템" 제목이 보입니다
- ✅ 로봇 상태 섹션이 표시됩니다
- ✅ 로봇 제어 버튼들이 보입니다
- ⚠️ 로봇이 연결되지 않아서 오류가 표시될 수 있습니다 (정상입니다)

---

## 2단계: UI 테스트 (로봇 없이도 가능)

### 버튼 클릭 테스트

로봇이 없어도 UI가 제대로 작동하는지 확인할 수 있습니다:

1. **"상태 새로고침" 버튼 클릭**
   - 예상: 오류 메시지가 표시됩니다 (로봇이 연결되지 않았기 때문)
   - 이것은 정상입니다! UI가 오류를 제대로 처리하는지 확인하는 것입니다

2. **"정지" 버튼 클릭**
   - 예상: 오류 메시지 표시 (정상)

3. **"말하기" 버튼 클릭**
   - 예상: 텍스트 입력 창이 나타납니다
   - 텍스트를 입력하고 확인을 누르면 오류가 표시됩니다 (정상)

### 오류 처리 확인

오류 메시지가 빨간색 박스로 표시되는지 확인하세요. 이것은 UI가 제대로 작동하고 있다는 증거입니다.

---

## 3단계: 테미 로봇 연결 준비 (로봇이 있는 경우)

### 로봇 정보 확인

테미 로봇이 있다면 다음 정보를 확인하세요:

1. **로봇 전원 켜기**
   - 로봇이 부팅되어 있는지 확인
   - 화면이 켜져 있는지 확인

2. **로봇 IP 주소 확인**
   - 로봇 화면: 설정 → 네트워크 → Wi-Fi → 연결된 네트워크
   - IP 주소를 메모하세요 (예: `192.168.1.100`)

3. **같은 네트워크 확인**
   - 컴퓨터와 로봇이 같은 Wi-Fi 네트워크에 연결되어 있는지 확인
   - 같은 네트워크여야 로컬 API를 사용할 수 있습니다

### 환경 변수 설정

1. 프로젝트 루트에 `.env.local` 파일을 생성합니다:

```bash
touch .env.local
```

2. `.env.local` 파일을 열고 다음 내용을 입력합니다:

```env
TEMI_ROBOT_IP=여기에_로봇_IP_주소_입력
TEMI_ROBOT_PORT=8080
```

예시:
```env
TEMI_ROBOT_IP=192.168.1.100
TEMI_ROBOT_PORT=8080
```

3. 개발 서버를 재시작합니다:
   - 터미널에서 `Ctrl + C`로 서버 중지
   - `npm run dev`로 다시 시작

### 연결 테스트

1. 브라우저에서 `http://localhost:3000` 새로고침
2. "상태 새로고침" 버튼 클릭
3. **성공 시**: 로봇 상태 정보가 표시됩니다
4. **실패 시**: 오류 메시지가 표시됩니다

---

## 4단계: 실제 로봇 제어 테스트 (로봇이 있는 경우)

### 기본 기능 테스트

로봇이 연결되었다면 다음을 테스트해보세요:

#### 1. 상태 조회
- "상태 새로고침" 버튼 클릭
- 배터리, 온라인 상태, 현재 위치 등이 표시되는지 확인

#### 2. 정지 명령
- 로봇이 움직이고 있다면 "정지" 버튼 클릭
- 로봇이 멈추는지 확인

#### 3. TTS 테스트
- "말하기" 버튼 클릭
- 텍스트 입력 (예: "안녕하세요")
- 로봇이 음성으로 말하는지 확인

#### 4. 위치 이동 (맵핑이 완료된 경우)
- 저장된 위치가 있다면 "이동" 버튼 클릭
- 로봇이 해당 위치로 이동하는지 확인

---

## 5단계: 문제 해결

### 일반적인 문제들

#### 문제 1: `npm install` 오류
**해결책**:
```bash
# Node.js 버전 확인 (v18 이상 필요)
node --version

# npm 캐시 클리어
npm cache clean --force

# 다시 설치
npm install
```

#### 문제 2: 개발 서버가 시작되지 않음
**해결책**:
- 포트 3000이 이미 사용 중일 수 있습니다
- 다른 터미널에서 실행 중인 프로세스 종료
- 또는 다른 포트 사용: `PORT=3001 npm run dev`

#### 문제 3: 로봇 연결 실패
**체크리스트**:
- ✅ 로봇이 부팅되어 있는가?
- ✅ 로봇과 컴퓨터가 같은 Wi-Fi에 연결되어 있는가?
- ✅ IP 주소가 정확한가?
- ✅ `.env.local` 파일이 올바르게 설정되었는가?
- ✅ 개발 서버를 재시작했는가?

#### 문제 4: CORS 오류
**해결책**:
- 로봇의 API가 CORS를 허용하지 않을 수 있습니다
- Next.js API 라우트를 통해 프록시를 만들어야 할 수 있습니다 (고급)

---

## 6단계: 모의 데이터로 테스트 (로봇이 없는 경우)

로봇이 없어도 UI를 테스트할 수 있도록 모의 데이터를 사용할 수 있습니다.

### 모의 API 클라이언트 만들기

`src/lib/temi-api-mock.ts` 파일을 만들어서 가짜 데이터를 반환하도록 할 수 있습니다.

이렇게 하면:
- UI가 제대로 표시되는지 확인 가능
- 버튼 클릭 시 동작 확인 가능
- 실제 로봇 없이도 개발 가능

---

## ✅ 오늘의 체크리스트

오늘 테스트를 마치면 다음을 확인할 수 있어야 합니다:

- [ ] 프로젝트가 정상적으로 실행됨 (`npm run dev` 성공)
- [ ] 브라우저에서 웹 인터페이스가 표시됨
- [ ] 버튼들이 클릭 가능함
- [ ] 오류 메시지가 제대로 표시됨
- [ ] (로봇이 있다면) 로봇 연결 성공
- [ ] (로봇이 있다면) 기본 제어 명령 테스트 완료

---

## 🎉 다음 단계

오늘의 테스트가 완료되면:

1. **문서 읽기**: `docs/temi-setup-guide.md`를 읽어서 로봇 설정 방법 익히기
2. **API 문서 확인**: `docs/api-reference.md`를 읽어서 사용 가능한 기능 확인
3. **기능 추가**: 필요한 기능을 추가하거나 UI를 개선하기

---

## 💡 팁

- **로봇이 없다면**: 모의 데이터를 사용하여 UI 개발을 계속할 수 있습니다
- **로봇이 있다면**: 실제 테스트를 통해 API가 제대로 작동하는지 확인하세요
- **문제가 생기면**: 터미널의 오류 메시지를 확인하세요. 대부분의 문제는 오류 메시지에 힌트가 있습니다

---

**행운을 빕니다! 🚀**

