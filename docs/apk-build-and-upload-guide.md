# 테미 로봇 APK 빌드 및 업로드 가이드

테미 로봇에 설치할 APK를 만들고 temi.center에 업로드하는 완전한 가이드입니다.

## 📋 목차

1. [APK 빌드 준비](#apk-빌드-준비)
2. [APK 빌드 방법](#apk-빌드-방법)
3. [테미 로봇에 APK 설치](#테미-로봇에-apk-설치)
4. [temi.center에 업로드](#temicenter에-업로드)
5. [문제 해결](#문제-해결)

---

## 🔧 APK 빌드 준비

### 1. Android Studio 프로젝트 확인

1. **Android Studio에서 프로젝트 열기**
   - `android/` 폴더를 Android Studio에서 열기
   - 프로젝트 동기화 완료 대기

2. **Temi SDK 확인**
   - `app/libs/` 폴더에 Temi SDK JAR 파일이 있는지 확인
   - 없으면 Temi SDK를 다운로드하여 추가

3. **의존성 확인**
   - `app/build.gradle`에서 필요한 라이브러리가 모두 추가되어 있는지 확인

### 2. 서명 키 생성 (릴리즈 빌드용)

릴리즈 APK를 만들려면 서명 키가 필요합니다:

```bash
# 키스토어 생성 (한 번만 실행)
keytool -genkey -v -keystore pangssi-restaurant.keystore -alias pangssi -keyalg RSA -keysize 2048 -validity 10000

# 정보 입력:
# - 비밀번호: 안전한 비밀번호 입력 (기억해두세요!)
# - 이름: 팡씨네 할머니집
# - 조직 단위: (선택사항)
# - 조직: (선택사항)
# - 도시: (선택사항)
# - 주/도: (선택사항)
# - 국가 코드: KR
```

생성된 `pangssi-restaurant.keystore` 파일을 안전한 곳에 보관하세요!

---

## 📦 APK 빌드 방법

### 방법 1: Android Studio에서 빌드 (권장)

#### 디버그 APK (테스트용)

1. **Android Studio 실행**
2. **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
3. 빌드 완료 후 **locate** 클릭
4. APK 파일 위치: `android/app/build/outputs/apk/debug/app-debug.apk`

#### 릴리즈 APK (배포용)

1. **서명 설정 추가**

`android/app/build.gradle` 파일에 다음을 추가:

```gradle
android {
    ...
    
    signingConfigs {
        release {
            storeFile file('../pangssi-restaurant.keystore')
            storePassword 'your_keystore_password'
            keyAlias 'pangssi'
            keyPassword 'your_key_password'
        }
    }
    
    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
```

⚠️ **보안 주의**: 비밀번호를 직접 입력하지 말고, 환경 변수나 별도 파일로 관리하세요!

2. **릴리즈 APK 빌드**
   - **Build** → **Generate Signed Bundle / APK**
   - **APK** 선택 → **Next**
   - 키스토어 파일 선택 및 비밀번호 입력
   - **release** 빌드 타입 선택
   - **Finish**
   - APK 위치: `android/app/build/outputs/apk/release/app-release.apk`

### 방법 2: 명령줄에서 빌드

```bash
cd android

# 디버그 APK 빌드
./gradlew assembleDebug

# 릴리즈 APK 빌드
./gradlew assembleRelease

# APK 파일 위치
# 디버그: app/build/outputs/apk/debug/app-debug.apk
# 릴리즈: app/build/outputs/apk/release/app-release.apk
```

---

## 📱 테미 로봇에 APK 설치

### 방법 1: ADB를 통한 설치 (권장)

#### 1단계: ADB 연결

```bash
# 로봇 IP로 연결 (이전에 연결했던 IP 사용)
adb connect 192.168.0.17:5555

# 연결 확인
adb devices
```

#### 2단계: APK 설치

```bash
# APK 설치
adb install app-debug.apk

# 또는 기존 앱이 있으면 재설치
adb install -r app-debug.apk

# 설치 확인
adb shell pm list packages | grep pangssi
```

#### 3단계: 앱 실행

```bash
# 앱 실행
adb shell am start -n com.pangssi.restaurant/.MainActivity

# 또는 로봇 화면에서 직접 실행
```

### 방법 2: USB를 통한 설치

1. **USB 케이블로 로봇과 컴퓨터 연결**
2. **USB 디버깅 허용** (로봇 화면에서)
3. **APK 설치**:
   ```bash
   adb install app-debug.apk
   ```

### 방법 3: 파일 전송 후 직접 설치

1. **APK 파일을 로봇으로 전송**
   ```bash
   # ADB로 파일 전송
   adb push app-debug.apk /sdcard/Download/
   
   # 또는 네트워크 공유 폴더 사용
   ```

2. **로봇 화면에서 설치**
   - 파일 관리자 앱 실행
   - `/sdcard/Download/app-debug.apk` 찾기
   - APK 파일 터치하여 설치
   - "알 수 없는 소스" 허용 (필요시)

---

## 🌐 temi.center에 업로드

### 1단계: temi.center 계정 준비

1. **temi.center 웹사이트 접속**
   - https://temi.center 접속
   - 계정 로그인 또는 회원가입

2. **개발자 계정 확인**
   - 개발자 권한이 있는지 확인
   - 필요시 개발자 계정 신청

### 2단계: 앱 정보 준비

업로드 전에 다음 정보를 준비하세요:

- **앱 이름**: 팡씨네 할머니집
- **패키지 이름**: com.pangssi.restaurant
- **버전**: 1.0.0
- **앱 설명**: 테미 로봇을 활용한 식당 자리 안내 시스템
- **스크린샷**: 앱 실행 화면 캡처
- **아이콘**: 앱 아이콘 이미지

### 3단계: APK 업로드

1. **temi.center 대시보드 접속**
   - 로그인 후 개발자 대시보드로 이동

2. **새 앱 추가**
   - "앱 추가" 또는 "Upload App" 버튼 클릭

3. **앱 정보 입력**
   ```
   앱 이름: 팡씨네 할머니집
   패키지 이름: com.pangssi.restaurant
   버전: 1.0.0
   설명: 테미 로봇을 활용한 식당 자리 안내 시스템입니다.
         손님이 입장하면 인원 수를 입력하고, 
         자동으로 적절한 테이블을 배정하여 테미 로봇이 안내합니다.
   ```

4. **APK 파일 업로드**
   - "APK 파일 선택" 클릭
   - `app-release.apk` 또는 `app-debug.apk` 선택
   - 파일 업로드 대기

5. **스크린샷 및 아이콘 업로드**
   - 앱 아이콘 이미지 업로드
   - 스크린샷 이미지 업로드 (여러 장 가능)

6. **카테고리 선택**
   - 적절한 카테고리 선택 (예: "음식점", "서비스" 등)

7. **제출 및 검토**
   - 모든 정보 입력 후 "제출" 클릭
   - temi.center 검토 대기 (보통 몇 일 소요)

### 4단계: 앱 배포

1. **검토 완료 후**
   - temi.center에서 검토 완료 알림 수신
   - 앱이 마켓플레이스에 등록됨

2. **테미 로봇에서 설치**
   - 테미 로봇 화면에서 temi.center 앱 실행
   - "팡씨네 할머니집" 검색
   - 설치 버튼 클릭

---

## 📝 APK 빌드 체크리스트

빌드 전 확인사항:

- [ ] Temi SDK가 `app/libs/`에 포함되어 있는가?
- [ ] `AndroidManifest.xml`의 패키지 이름이 올바른가?
- [ ] 앱 이름이 `strings.xml`에 올바르게 설정되어 있는가?
- [ ] URL이 프로덕션 URL로 설정되어 있는가? (릴리즈 빌드)
- [ ] 서명 키가 올바르게 설정되어 있는가? (릴리즈 빌드)
- [ ] 앱 아이콘이 설정되어 있는가?

---

## 🔍 문제 해결

### 빌드 오류

**오류: "SDK location not found"**
```bash
# local.properties 파일 생성
echo "sdk.dir=/Users/사용자명/Library/Android/sdk" > android/local.properties
```

**오류: "Temi SDK not found"**
- Temi SDK JAR 파일을 `app/libs/` 폴더에 추가
- `build.gradle`에 의존성 추가 확인

### 설치 오류

**오류: "INSTALL_FAILED_UPDATE_INCOMPATIBLE"**
```bash
# 기존 앱 제거 후 재설치
adb uninstall com.pangssi.restaurant
adb install app-debug.apk
```

**오류: "INSTALL_FAILED_INSUFFICIENT_STORAGE"**
- 로봇의 저장 공간 확인
- 불필요한 앱 제거

### temi.center 업로드 오류

**오류: "Package name already exists"**
- 다른 패키지 이름 사용
- 또는 기존 앱 업데이트

**오류: "APK signature verification failed"**
- 올바른 서명 키로 빌드했는지 확인
- 릴리즈 빌드 사용

---

## 💡 팁

1. **디버그 vs 릴리즈**
   - 테스트: 디버그 APK 사용
   - 배포: 릴리즈 APK 사용 (서명 필요)

2. **버전 관리**
   - 업데이트 시 `versionCode`와 `versionName` 증가
   - `app/build.gradle`에서 수정

3. **프로덕션 URL 설정**
   - 릴리즈 빌드 전에 `MainActivity.java`의 URL을 프로덕션 URL로 변경

4. **앱 아이콘**
   - `app/src/main/res/mipmap-*/ic_launcher.png` 파일 교체
   - 다양한 해상도 제공 (mdpi, hdpi, xhdpi, xxhdpi, xxxhdpi)

---

## 📚 참고 자료

- [Android APK 빌드 공식 문서](https://developer.android.com/studio/build)
- [temi.center 개발자 가이드](https://temi.center)
- [테미 SDK 문서](https://www.robotemi.com)

---

**이제 APK를 만들고 테미 로봇에 설치할 준비가 되었습니다!** 🚀

